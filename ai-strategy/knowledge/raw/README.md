# Raw

Untouched source material per board member, one subfolder per person: `raw/[person-slug]/`. Populated by the `ingest-resource` skill. Never hand-edit — always re-ingest from source so the raw copy stays authentic.
