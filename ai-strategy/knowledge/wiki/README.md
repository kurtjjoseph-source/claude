# Wiki

Synthesized per-person profiles distilled from `knowledge/raw/`: `wiki/[person-slug].md`. This is what the `ask-the-board` skill reads. Regenerated (not appended to) by the `ingest-resource` skill whenever new raw material comes in for that person.
