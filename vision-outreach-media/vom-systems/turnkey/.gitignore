.vercel
.env.local
node_modules
.DS_Store

# Forge output. Both are regenerated by `forge/build.py` from the engine and the
# archetypes, so the sources are the thing worth tracking, not the builds.
forge/public/
forge/demos/

# Real env files never enter the repo — .env.example is the template.
.env
